import React from 'react';
import { NavLink } from 'react-router-dom';

interface Props {}

const Header: React.FC<Props> = () => (
  <div>
    <NavLink to="/">hasdfasdfi</NavLink>
  </div>
);

export default Header;
